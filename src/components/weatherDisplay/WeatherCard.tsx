import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function WeatherCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>9/14/2022</CardTitle>
      </CardHeader>
      <CardContent>test</CardContent>
    </Card>
  );
}
